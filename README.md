# 6.5-S3-MinIO
Для выполнения первых двух заданий используется файл class_S3.py, а также log_pass.env. 
В нем находятся все методы, которые вызываются, для выполнения заданий. 
Скриншоты во вложении.

Первое задание:
Добавьте метод list_files(), который будет возвращать список объектов в бакете.
Добавьте метод file_exists(), который должен возвращать булевый ответ на запрос о наличии файла с определенным именем.
В классе, который дан в стэпе, уже имеются эти методы, просто их нужно применить.


Второе задание:

Настройте bucket policy таким образом, чтобы:
1)Любой мог читать файлы из определенного бакета.
Имеется возможность скачивания по ссылке локально без авторизации: http://localhost:9000/bucketmaks/sales_data.csv, тем самым подтверждает, что любой мог читать файлы из бакета.

2)Только вы могли писать в нее.
Так как имеются права администратора локально, то это условие уже выполняется.


Включите версионирование в бакете и загрузите файл с одним именем несколько раз, а после скачайте его предыдущую версию.


Настройте lifecycle policy таким образом, чтобы через 3 дня объекты автоматически удалялись.
Сегодня (02.01), когда я делаю задание, меняю дату локально на 06.01, проверяя корректность метода.

